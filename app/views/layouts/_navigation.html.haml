%nav.navbar.navbar-default.navbar-fixed-top
  .container.navigation__container
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to 'Qiitan', root_path, class: 'navbar-brand'
    .collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          %li.dropdown
            = link_to icon('fas', 'pencil-alt', '投稿する'), new_article_path, class: 'btn btn-primary btn-block btn-sm'
          %li.dropdown
            = link_to '#', class: 'dropdown-toggle', role: 'button', data: { toggle: 'dropdown' } do
              = current_user.username
              %span.caret
            %ul.dropdown-menu{ role: 'menu' }
              %li
                = link_to destroy_user_session_path, method: 'delete' do
                  = icon('fas', 'sign-out-alt', 'ログアウト')
